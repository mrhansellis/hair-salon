@{
  Layout = "_Layout";
}

@model HairSalon.Models.Stylist
@using HairSalon.Models

<h2>Stylist Dossier</h2>
<hr />
<p>@Html.DisplayNameFor(model => model.StylistName): @Html.DisplayFor(model => model.StylistName)</p>

@if (@Model.Clients.Count == 0)
{
  <p>This stylist doesn't have any clients yet.</p>
}
else
{
  <h3>Clients</h3>
  <ul>
  @foreach (Client client in Model.Clients)
  {
    <li>@Html.ActionLink($"{client.ClientName}", "Details", "Clients", new { id = client.ClientId })</li>
  }
  </ul>
}
<p>@Html.ActionLink("Add client", "Create", "Clients")
<p>@Html.ActionLink("Edit Stylist", "Edit", new { id = Model.StylistId})</p>
<p>@Html.ActionLink("Delete Stylist", "Delete", new { id = Model.StylistId} )</p>
